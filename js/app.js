let arrayPrices = [200, 550, 4000, 23, 58, 5000, 485, 711];
let arraySum = (array) =>
	{
		let sum = 0;
		for (let value of array) 
			sum += value;
		console.log (sum);
	}

const bonus = (array) => { Math.round (
	for (let value of prices) {
		if (value >=)
		console.log(value * 2);
		}
	);

};

